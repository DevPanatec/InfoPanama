# 🔄 Resetear Tutorial

Abre el navegador en http://localhost:3000/admin/dashboard

Luego abre la consola (F12) y ejecuta:

```javascript
localStorage.removeItem('hasSeenTutorial')
location.reload()
```

El tutorial aparecerá de nuevo.
